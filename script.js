$(document).ready(function() {
  $("#generate").on("click", function(){
    // Only change code below this line.
    $(".quote").html("The quote has changed.");
    // Only change code above this line.
  });
});